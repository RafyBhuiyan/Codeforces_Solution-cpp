#include<stdio.h>
int main(){
    int i,n,sum=0,a,ans=0,j;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        for(j=0;j<3;j++){
            scanf("%d",&a);
            if(a==1)
                sum++;
        }
        if(sum>1)
            ans++;
            sum=0;
    }
    printf("%d",ans);
}
