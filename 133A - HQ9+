#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    string ra;
    getline(cin,ra);
    int l = ra.size();
    for(int i = 0; i<l ; i++)
    {
        if(ra[i]=='H' || ra[i]=='Q' ||ra[i]=='9' )
        {
            cout<< "YES";
            break;
        }
        else if(i==l-1)
        {
            cout<< "NO";
        }
    }
    return 0;
}
