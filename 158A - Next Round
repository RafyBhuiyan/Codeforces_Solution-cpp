#include<stdio.h>
int main()
{
    int x,y,i,sum=0;
    scanf("%d%d",&x,&y);
    int ra[x];
    for(i=0; i<x; i++)
    {
        scanf("%d",&ra[i]);
    }
    for(i=0; i<x; i++)
    {
        if(ra[i]>=ra[y-1] && ra[i]!=0)
            sum++;
    }
     printf("%d",sum);
    return 0;
}
