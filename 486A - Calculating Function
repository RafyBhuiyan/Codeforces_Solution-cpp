#include<bits//stdc++.h>
using namespace std;
int main(){
    long long int num,sum=0;
    int x=1;
    scanf("%lld",&num);
    if(num%2==0)
        sum=num/2;
    else
        sum=-1*((num/2) +1);
    printf("%lld",sum);
    return 0;
}
