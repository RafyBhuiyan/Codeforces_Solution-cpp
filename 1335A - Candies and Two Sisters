#include<bits/stdc++.h>
#include<string.h>
using namespace std;
int main()
{
   int x,a,sum;
   cin>> x;
   while(x--)
  {
        cin>> a;
   if(a%2==0)
     sum=a/2 -1;
   else
    sum=a/2;
    cout<<sum<<endl;
  }
    return 0;
}
