#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    int m,n,sum=0;
    cin>> m>>n;
    while(1){
        if(m==0 || n==0)
            break;
        sum++;
        m--;
        n--;
    }
    if(sum%2==0)
        cout<< "Malvika";
    else
        cout<< "Akshat";
    return 0;
}
